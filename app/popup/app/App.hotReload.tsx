import React from 'react';
import Wallet from "@module/Wallet/Wallet";

const AppHotReload: React.FunctionComponent = () => {
  return (
    <Wallet />
  );
};

export default React.memo(AppHotReload);
