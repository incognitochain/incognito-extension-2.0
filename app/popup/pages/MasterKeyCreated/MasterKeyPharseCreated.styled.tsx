import { PrimaryButtonStyled } from "@popup/components/Core/Buttons";
import styled from "styled-components";

const CircleIconContainer = styled.div`
  margin-top: 180px;
`;

const IncognitoContainer = styled.div`
  margin-top: 16px;
`;

const PrimaryButtonContaniner = styled(PrimaryButtonStyled)`
  margin-top: 120px;
`;

const ExtensionTextContainer = styled.p`
  text-align: center;
  margin-top: 8px;
`;

export { CircleIconContainer, IncognitoContainer, ExtensionTextContainer, PrimaryButtonContaniner };
