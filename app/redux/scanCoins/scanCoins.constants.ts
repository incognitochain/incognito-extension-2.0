export const ACTION_FETCHING_SCAN_COINS = "[scan coins] Fetching coins";
export const ACTION_FIRST_TIME_SCAN_COINS = "[scan coins] First time scan coins";

export enum ScanCoinsActionType {
  FETCHING = "[scan_coins] Fetching coins",
  FIRST_TIME_SCAN_COINS = "[scan coins] First time scan coins",
}
